change the directory to src/frontend
command: npm install -> installs node modules
add a .env in the /frontend with : VITE_API_BASE_URL=http://localhost:8000
comand to start -> npm run dev

open: localhost:5173
